---
# tasks file for ansible-role-prerequisites

- name: Install Prerequisites
  yum:
    name:
    - vim
    - net-snmp
    - git
    update_cache: yes

- name: Set hostname
  hostname:
    name: "{{ server_name }}"
